# Dev Handoff: AI SDK Migration + useChat + Agent-Ready Architecture

> Generated by /dev on 2026-04-10 | Mode: plan-driven | Type: integration | Tags: none

## Source Plan

docs/plans/ai-sdk-migration-usechat-agent-ready-20260410-155411.md

## Implementation Summary

Migrated all LLM calls from `@google/generative-ai` to the Vercel AI SDK (`ai` + `@ai-sdk/google`). The web chat frontend now uses `useChat` from `@ai-sdk/react` with real-time token streaming via `streamText` + `toUIMessageStreamResponse`. The Telegram bot path stays non-streaming with `generateText`. Structured output for `parseAddMessage` uses `generateText` with `Output.object()` and a Zod schema (`ParsedAddSchema`).

## Files Written

| File | Action | Status |
|------|--------|--------|
| `src/types/idea.ts` | modify | done |
| `src/services/ai.ts` | modify | done |
| `app/api/chat/route.ts` | modify | done |
| `app/_components/ChatBox.tsx` | modify | done |
| `src/config.ts` | modify | done |
| `.env.example` | modify | done |
| `package.json` | modify | done (via bun add/remove) |

## Steps Completed / Skipped / Failed

- Phase 0: Install dependencies — done (bun add @ai-sdk/google @ai-sdk/react zod)
- Phase 1: Add Zod schema to src/types/idea.ts — done (checkpoint: passed)
- Phase 2: Migrate src/services/ai.ts to AI SDK — done (checkpoint: passed, fixed async convertToModelMessages)
- Phase 3: Rewrite chat route for streaming — done (checkpoint: passed)
- Phase 4: Migrate ChatBox.tsx to useChat — done (checkpoint: passed)
- Phase 5: Cleanup — done (removed geminiApiKey, updated .env.example, removed @google/generative-ai)

## Build Result

passed

## Open Questions (forwarded from plan)

- [ ] Agent evolution: when building an agent, `streamQueryResponse` becomes the extension point — replace `streamText` with `ToolLoopAgent`. The route handler and `useChat` frontend won't need changes, but data access (currently a flat text blob of all ideas in the system prompt) will need tool-based retrieval for scale

## Testing Strategy

- [ ] **parseAddMessage**: Send a test message via Telegram `/add Cenar en un restaurante italiano por $30` — verify structured `ParsedAdd` is stored in Google Sheets
- [ ] **parseAddMessage fallback**: Send malformed input to verify fallback fires (raw input as title)
- [ ] **answerQuery (bot)**: Send `/query ¿Qué opciones baratas hay?` via Telegram — verify text response
- [ ] **Web chat streaming**: Open `/chat` in browser, submit a question, verify tokens stream in real time
- [ ] **Web chat stop**: Click "Detener" during streaming — verify stream stops
- [ ] **Web chat multi-turn**: Send multiple messages — verify conversation history is preserved
- [ ] **Error state**: Remove `GEMINI_API_KEY` temporarily — verify error message appears in ChatBox
- [ ] **Build**: `bun run build` passes with no type errors

## /pre-pr Instructions

- [ ] Verify `GEMINI_API_KEY` is set in Vercel environment variables
- [ ] Verify bot `/add` returns valid ParsedAdd (test with Telegram)
- [ ] Verify bot `/query` returns text response (test with Telegram)
- [ ] Verify web chat streams tokens in real time (test in browser)
- [ ] Verify `maxDuration = 55` is present on chat route
- [ ] Verify `googleapis` stays in `serverExternalPackages` in `next.config.ts`
- [ ] Verify no imports from `src/bot/` in web/Next.js code

## Code Quality Notes (advisory)

- `ideasText` formatting block is duplicated between `answerQuery` and `streamQueryResponse` in `src/services/ai.ts` — consider extracting a `formatIdeas()` helper
